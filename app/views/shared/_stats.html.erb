<!-- >
Common partial for profile and home page
* in profile page @user would be available from controller
* in home page @user is null, so current_user is used to load user form session
-->
<% @user ||= current_user %>
<div class = "stats">
    <a href="<%= following_user_path(@user) %>">
        <strong id="following" class="stat">
            <%= @user.following.count %>
        </strong>
        following
    </a>
    <a href="<%= followers_user_path(@user) %>">
        <strong id="followers" class="stat">
            <%= @user.followers.count %>
        </strong>
        followers
    </a>    
</div>

