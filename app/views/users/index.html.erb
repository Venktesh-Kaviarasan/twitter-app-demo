<% provide(:title, "All Users") %>

<h1> All Users </h1>

<ul class="users">
    <% @user.each do |x| %>
        <li>
            <%= gravatar_for x, size: 50 %>
            <%= link_to x.name, x %>
        </li>
    <% end %>
</ul>